<section id="quickstart" data-searchable data-search="quick start three steps">
    <div class="section-header">
        <span class="section-icon">🚀</span>
        <div>
            <h2>Quick start — 3 steps to your first section</h2>
            <p>If you already have the template installed in your WordPress theme. Otherwise jump to <a href="#install">Install</a> first.</p>
        </div>
    </div>

    <div class="grid grid-3 step-stack">
        <div class="card reveal">
            <span class="pill pill-cowork" data-only="cowork">Cowork</span>
            <span class="pill pill-code" data-only="code">Claude Code</span>
            <h3 style="margin-top: 12px;">1. Open the project</h3>
            <p data-only="cowork">In Cowork, click "Select folder" and navigate to your theme directory (e.g. <code>D:\laragon\www\client\wp-content\themes\theme-slug\</code>).</p>
            <p data-only="code">In your terminal: <code>cd D:\laragon\www\client\wp-content\themes\theme-slug</code> then <code>claude</code>.</p>
        </div>

        <div class="card reveal">
            <span class="pill">Either platform</span>
            <h3 style="margin-top: 12px;">2. Run setup once</h3>
            <p>Type <code>/setup-claude</code>. Claude auto-detects the theme structure, asks 2-3 questions (project name, optional Figma URL), and configures everything in ~2 minutes.</p>
        </div>

        <div class="card reveal">
            <span class="pill">Either platform</span>
            <h3 style="margin-top: 12px;">3. Build a section</h3>
            <p>Paste in chat:</p>
            <div class="codeblock"><pre>@home-hero https://figma.com/.../1-2</pre></div>
            <p style="font-size: 0.8125rem;">Claude reads Figma, writes the section, replies with judgment calls. Hard-refresh the live page to preview.</p>
        </div>
    </div>

    <div class="callout callout-info" style="margin-top: var(--space-6);">
        <span class="callout-icon">💡</span>
        <div class="callout-body">
            <strong>Don't know how to phrase what you want?</strong> Click the floating <span style="color: var(--color-accent);">✨</span> button (bottom-right) or use the <a href="#prompt-builder">AI Prompt Builder</a>. Type rough English, get the perfect prompt.
        </div>
    </div>
</section>
